import React, { useState } from "react";

const TopNavber = () => {
  return (
    <div
      className={`w-full h-16 bg-[#FFFFFF] flex flex-row justify-between px-5 items-center `}></div>
  );
};

export default TopNavber;
